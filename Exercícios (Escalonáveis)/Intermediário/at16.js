const r = require('prompt-sync')
const prompt = r()

// Determine se uma data (dia e mês) é feriado nacional

let dia = Number(prompt('Digite a data de hoje (dia): '))
let mes = Number(prompt('E digite o seu mês: '))

